# 더미(mock) 모드 토글.
#  - true  : MSW(Mock Service Worker)가 /api 요청을 더미 데이터로 응답합니다. (기본값)
#  - false : MSW 를 끄고 실제 백엔드(VITE_API_BASE_URL)로 요청합니다.
VITE_USE_MOCK=true

# API base URL. axios 와 MSW 가 공통으로 사용합니다.
# 실제 백엔드가 별도 도메인이면 전체 URL(예: https://api.example.com)로 변경하세요.
VITE_API_BASE_URL=/api
