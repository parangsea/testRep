# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This project is not yet scaffolded. As of this writing the repository contains only `README.md` (title: `testRep`) — there is no source code, no dependency manifest, and no build, lint, or test tooling.

There are therefore no build/test/run commands to document and no architecture to describe yet.

## Next steps for this file

Once the project is scaffolded (language/framework chosen, dependency manifest added, build and test commands working), re-run `/init` to regenerate this file with:

- Common commands (build, lint, test — including how to run a single test)
- The high-level architecture that spans multiple files
